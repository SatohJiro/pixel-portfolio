const fs = require('fs');
const path = require('path');
const zlib = require('zlib');

// Let's create a pure Node script that decodes the JPEG/PNG or uses a transparent PNG generator
console.log('Script started');
